# Munch Assistant Requirements
# Python 3.6+ required

# Audio support (REQUIRED for the full experience)
pygame>=2.0.0

# curses module is included in standard Python library (Unix/Linux/Mac)
# For Windows, you need both pygame AND windows-curses:
# windows-curses>=2.3.0  # Uncomment if on Windows
